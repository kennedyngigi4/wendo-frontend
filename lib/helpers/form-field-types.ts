export enum FormFieldType {
    INPUT = "input",
    PHONE = "phone",
    SELECT = "select",
    TEXTAREA = "textarea",
    CHECKBOX = "checkbox",
    RADIO = "radio",
    TOGGLE = "toggle",
    DATETIME = "datetime",
}